extends layout

block content
  h1= title
  
  ul
  each accountGroup in accountGroup_list
    li 
      a(href=accountGroup.url) #{accountGroup.Name}  
      | (#{accountGroup._id})

  else
    li There are no account groups.
