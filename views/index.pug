extends layout

block content
  h1= title
  p Welcome to #[em BudgetWeb] dep.
  p time #{time}

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The base has the following record counts:

    ul
      li #[strong Tags:] !{data.tag_count}
      li #[strong Orders:] !{data.order_count}
